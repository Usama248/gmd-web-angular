import { FileSizeCheckDirective } from './file-size-check.directive';

describe('FileSizeCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSizeCheckDirective();
    expect(directive).toBeTruthy();
  });
});
